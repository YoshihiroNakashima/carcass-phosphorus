# Carcass-derived phosphorus supply: global analysis

Code accompanying the manuscript:

**"Conserved in quantity, lost in structure: megafauna loss reshapes the spatial heterogeneity of carcass-derived phosphorus supply"**
Yoshihiro Nakashima (Nihon University)

The analysis is entirely computational. It estimates the global supply of phosphorus (P) returned to landscapes through mammal carcasses, its body-size composition, and its spatial heterogeneity, and contrasts continents that lost their megafauna (South America, Australia) with one that retained it (Africa) within matched WWF biomes.

---

## Repository contents

```
analysis_pipeline.R      Full pipeline in sequential blocks (1-7), runnable top to bottom.
data/                    Place MODIS NPP GeoTIFFs and the malddaba .rdata here (not redistributed).
out/                     Provided intermediate outputs (grid-level CSVs); also written by Block 1/3.
figure/                  Figure outputs (written by Block 6).
report_values.txt        Reference output of Block 7 (all reported values).
LICENSE                  MIT (code).
LICENSE-data             CC BY 4.0 (data files generated by this study).
```

## Requirements

R (>= 4.1) with packages: `terra`, `sf`, `dplyr`, `tidyr`, `viridis`.

```r
install.packages(c("terra", "sf", "dplyr", "tidyr", "viridis"))
```

## How to run

The pipeline is organised into blocks that mirror the analysis stages. Each block is a function in `analysis_pipeline.R`; later blocks read the outputs of earlier ones.

```r
source("analysis_pipeline.R")

# Option A: reproduce all results from the provided intermediate files
run_all(from_intermediates = TRUE)

# Option B: full rebuild from the raw inputs (requires all raw datasets below)
run_all(from_intermediates = FALSE)
```

Block 7 prints every value reported in the manuscript, each tagged (e.g. `[[GLOBAL_FLUX_MEDIAN]]`), and also writes `report_values.txt`.

The Monte Carlo iteration count is set to `N_ITER <- 500` in the CONFIG section (top of the script); this is the setting used for the reported confidence intervals.

---

## Data availability

The analysis uses only publicly available datasets. To keep the repository self-contained and reproducible, the grid-level intermediate files derived from these sources are included (see `out/`), so the full pipeline can be reproduced from Block 4 onward **without** the raw inputs.

| Dataset | Role | Redistributed here? | Access |
|---|---|---|---|
| Greenspoon et al. (2023), wild land mammal biomass | per-species global abundances | yes | public dataset / repository |
| MODIS MOD17A3HGF NPP (2015-2020) | productivity for within-range allocation | no (large; openly available) | LP DAAC / Google Earth Engine |
| malddaba demographic database | life-history records | no (use original source) | malddaba |
| Etard et al., consolidated trait data | adult body mass, taxonomic order | yes | Dryad doi:10.5061/dryad.05qfttfdq |
| WWF Terrestrial Ecoregions of the World (Olson et al. 2001) | realm/biome stratification | yes | WWF |
| IUCN Red List range polygons | species ranges | **no — see below** | IUCN (registration required) |

### IUCN range polygons (important)

IUCN Red List spatial data (terrestrial mammal range polygons) are **freely available to registered users** but **cannot be redistributed** under the IUCN Red List Terms of Use. They are therefore **not** included in this repository.

To support full reproducibility without the raw polygons, the repository provides:

1. **Access instructions and version.** Download the terrestrial mammal ranges from the IUCN Red List spatial data download (https://www.iucnredlist.org/resources/spatial-data-download). Record the version and download date you use. This study used IUCN Red List version `<FILL IN>` (downloaded `<FILL IN>`).
2. **The processing script.** Block 1 (`run_block1_rasterize_ranges`) documents exactly how the polygons are converted to the 0.5-degree grid (extant ranges, native/reintroduced origin, `touches = TRUE`).
3. **The derived intermediate.** The grid-level species-occupancy table produced by Block 1 is provided as `out/cell_species_long.csv.zip` (the uncompressed CSV exceeds GitHub's file-size limit). The pipeline reads it directly from the `.zip` — no manual extraction is needed. All downstream blocks (4-7) read this table, so every reported figure and value can be reproduced from it without the raw IUCN polygons.

If you wish to regenerate the occupancy table from scratch, obtain the IUCN polygons as above, set `PATH_IUCN_POLYGON` in the CONFIG section, and run Block 1 (which writes the uncompressed `out/cell_species_long.csv`).

---

## Pipeline overview

| Block | Stage | Key output |
|---|---|---|
| 1 | Rasterize IUCN ranges to the 0.5-degree grid | `cell_species_long.csv`, `grid_template.csv` |
| 2a | Body-mass master (Etard trait data) | `body_mass_master.csv` |
| 2b | Life tables from malddaba | `calibration_table.csv` |
| 2c | Extrapolate mortality parameters to all species | `predictions_all.csv` |
| 2d | Turnover (annual population turnover) | `turnover_final.csv` |
| 2e | Calibration diagnostics (Supplementary Fig. S1) | `figure/fig_calibration.png` |
| 3 | Density maps (NPP-weighted within-range allocation) | `cell_density_both.csv` |
| 4 | P-supply flux (skeletal P x retention) — core | `species_flux_props_NP.csv` |
| 5 | Uncertainty propagation (Monte Carlo) | `cell_flux_ci_P.csv` |
| 6 | Figures | `figure/*.png`, `figure/*.csv` |
| 7 | Reported values | `report_values.txt` |

### Method summary

Supply is defined as the **skeletal** P retained at the death site after a uniform local-loss (retention) coefficient; soft-tissue P is assimilated by consumers and exported, so it is not counted as local supply (it is retained only as a reference quantity for whole-body P and carcass N:P). Per-species abundances (Greenspoon) are distributed within each IUCN range in proportion to NPP, multiplied by turnover (from survivorship) and by skeletal P per carcass (integrated over the age-at-death distribution). Spatial heterogeneity is the variance-to-mean ratio (VMR) of a compound-Poisson deposition process, `E[P^2]/E[P]`, computed per cell. Continents are compared within matched WWF biomes.

---

## License

- **Code:** MIT (see `LICENSE`).
- **Data files generated by this study** (the intermediate CSVs in `out/`): CC BY 4.0 (see `LICENSE-data`). Third-party datasets remain under their original licenses and terms; in particular, IUCN range polygons are not redistributed here.

## Citation

If you use this code, please cite the manuscript (full citation and archived DOI to be added upon publication).
